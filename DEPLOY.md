# 배포 순서 (약 15분)

## 0. 도구 파일 — 이미 들어 있습니다

`lab/index.html`에 작동하는 실습 도구가 들어 있어서, 그대로 올려도 404는 나지 않습니다.
직접 만드신 `ai_literacy_lab.html`이 따로 있다면 이 파일을 **덮어쓰세요.** 파일명은 `index.html`을 유지해야 합니다.

```
저장소/
├── index.html          ← 포트폴리오 (이미 있음)
├── lab/
│   └── index.html      ← 작동하는 도구 (덮어써도 됨)
├── README.md
├── DEPLOY.md
└── .nojekyll
```

업로드 후 "Open the tool" 링크를 눌러 실제로 열리는지 한 번 확인하세요.

## 1. 채워야 하는 자리

`index.html`과 `README.md`에서 대괄호 `[ ]`로 표시된 곳입니다. 페이지에서는 빨간 글씨로 보입니다.

- `[Full Name]` / `[이름]`, `[학과]`
- `[email]` / `[이메일]`
- `[username]`, `[repo]` — 깃허브 아이디와 저장소 이름
- **실패 사례와 고친 방법** — 가장 중요합니다. 아래 2번 참고
- `[Sogang club name]` — 데모 나이트 공동 개최를 제안할 교내 동아리 2곳

## 2. 실패 사례를 반드시 채우세요

포트폴리오에서 심사자가 제일 눈여겨볼 두 문장입니다. "잘 만들었다"는 누구나 쓰지만,
**무엇이 망가졌고 어떻게 고쳤는지**는 실제로 교실에 있어 본 사람만 씁니다.

좋은 예시의 형태:

> 실제로 망가진 지점: 학생들이 "게임 만들어줘" 한 줄만 넣고, 밋밋한 결과를 받고,
> AI가 원래 이 정도라고 결론 내렸다.
> 고친 방법: 프롬프트를 보내기 전에 "누가 / 무엇을 / 어떤 느낌으로" 세 칸을 먼저
> 채우게 강제했다.

## 3. GitHub Pages 켜기

1. GitHub에서 새 저장소를 만듭니다 (Public). 이름 예: `saenggak-han-kan`
2. 이 폴더의 파일을 전부 업로드합니다. 웹에서 **Add file → Upload files**로 드래그하면 됩니다.
3. **Settings → Pages**
4. Source: **Deploy from a branch**, Branch: **main**, 폴더: **/ (root)** → Save
5. 1~2분 뒤 `https://[아이디].github.io/[저장소]/` 로 열립니다.

`.nojekyll` 파일은 지우지 마세요. GitHub이 파일을 임의로 가공하지 않게 막아 줍니다.

## 4. 올린 뒤 확인

- [ ] `/` 열림
- [ ] `/lab/` 열리고 도구가 실제로 작동함
- [ ] 모바일에서 열어보기 (심사자가 폰으로 볼 수 있음)
- [ ] EN 버튼 눌렀을 때 한국어가 안 남아 있는지
- [ ] 대괄호 `[ ]`가 하나도 안 남았는지 — 페이지 검색(Ctrl+F)으로 `[` 찾기

## 5. 이력서 PDF도 같이 올리기

이력서를 PDF로 내보내 저장소 루트에 `resume.pdf`로 올리면
포트폴리오의 "Résumé (PDF)" 링크가 바로 살아납니다.

## 6. 지원서에 넣을 링크

- Claude로 만든 것: `https://[아이디].github.io/[저장소]/lab/`
- 전체 소개: `https://[아이디].github.io/[저장소]/`
- 코드: `https://github.com/[아이디]/[저장소]`

세 개를 다 넣으세요. "만들었다"는 주장과 "여기 있다"는 증거는 심사에서 완전히 다르게 취급됩니다.
